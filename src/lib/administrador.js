var submit;
function checkForm(form)
{
    if("Agregar permiso" != submit.value)
        alert(submit.value);
    else
        alert('mentirosa traicionera');
    return false;
}

function Clicked(button)
{
    submit= button ;
}